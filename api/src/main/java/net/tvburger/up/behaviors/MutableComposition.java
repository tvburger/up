package net.tvburger.up.behaviors;

/**
 * Represents an object that is mutable. Most objects are created immutable, but some are explicitly intended to
 * change through out their life. That means there are getters and setters available. When keeping a reference to
 * this object, the value might have changed.
 */
public interface MutableComposition {
}
